.navbar.navbar-default.navbar-fixed-top(role='navigation', ng-controller="headerCtrl", id="header")
			.container-fluid
				.navbar-header
					button.navbar-toggle(type='button',data-toggle='collapse',data-target='.navbar-collapse')
						span.sr-only Toggle navigation 	
						span.icon-bar
						span.icon-bar
						span.icon-bar
					a.navbar-brand(href='#') Encontrainador
				.navbar-collapse.collapse
					ul.nav.navbar-nav.navbar-right(ng-show="!open")
						li 
							a(ng-click="logIn('signUp')") Sign Up
						li 
							a(ng-click="logIn('logIn')") Log In

					ul.nav.navbar-nav.navbar-right(ng-show="open")
						li 
							div.searcher
								input(type="text",placeholder="Buscar ...")
								span(class="glyphicon glyphicon-search" aria-hidden="true")
						li 
							a(ng-click="report()") Reportar
						li 
							a(ng-click="miAcount()") Mi Cuenta